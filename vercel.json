{
  "framework": "nextjs",
  "buildCommand": "prisma generate && prisma db push --skip-generate && node prisma/seed.mjs && npm run build",
  "outputDirectory": ".next",
  "installCommand": "npm install"
}
